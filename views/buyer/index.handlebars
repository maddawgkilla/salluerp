{{> navbar}}


<div class="container mt-5">
    <div class="content">
        <h1>All Buyers</h1>
        {{#each foundBuyers}}
        <a href="buyers/{{this.buyer_name}}">
            <div class="panel row">
                <span class="mr-auto ml-auto">{{this.buyer_name}}</span>
            </div>
        </a>
        {{/each}}
    </div>
</div>